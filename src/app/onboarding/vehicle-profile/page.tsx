import VehicleProfile from '@components/Onboarding/VehicleProfile'
import React from 'react'

const page = () => {
  return (
    <main>
      <VehicleProfile/>
    </main>
  )
}

export default page
